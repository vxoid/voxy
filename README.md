# Voxy proxy server | v0.0.1
## Changelog
- Init
## Usage
- Run the proxy server, you can build exe using basic cargo build
- Send to proxy server regular http packet but with Host header set to host you wanna access